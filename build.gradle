group 'io.github.diogocp.secpassman'
version '1.0-SNAPSHOT'

subprojects {
    repositories {
        mavenCentral()
    }
}

task clean {
    doLast{
        delete ('servers.tmp/', 'config.properties')
    }
}
